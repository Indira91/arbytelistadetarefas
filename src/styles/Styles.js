
const Styles = ({
    textum: {
        fontSize: 45,
        textAlign: 'center',
        fontWeight: "bold"
    },
    textdois: {
        fontSize: 35,
        textAlign: 'center',
        fontWeight: "bold"
    },
    textres: {
        fontSize: 20,
        textAlign: 'center',
        fontWeight: "bold"
    }
})

export default Styles;
